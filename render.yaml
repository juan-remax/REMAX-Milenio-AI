services:
  - type: web
    name: remax-milenio-ai
    runtime: python
    plan: free
    buildCommand: pip install -e ".[dev]"
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port $PORT

databases:
  - name: remax-milenio-db
    plan: free
    databaseType: postgresql
